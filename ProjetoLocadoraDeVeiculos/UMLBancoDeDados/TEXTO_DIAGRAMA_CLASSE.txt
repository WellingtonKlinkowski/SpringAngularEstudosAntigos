Fazer um sistema para gestão de uma locadora de veículos
O sistema deve ter tbm produtos agregados como seguros, seguro de vida, cadeirinhas para locar, cinto para pets, consumíveis
como água, refri e comidas, deve ter taxas, juros por atraso, taxas de limpeza

TB_VEICULO
	- VEI_ID SERIAL PK
	- VEI_MARCA VARCHAR(30) NOT NULL
	- VEI_NOMVEI VARCHAR(50) NOT NULL
	- VEI_TIPVEI INT FK NOT NULL *
	- VEI_COR VACHAR(30) NOT NULL
	- VEI_QTDLUGARES INT NOT NULL
	- VEI_CAMBIO VARCHAR(20) --- ENUM NO BACKEND TIPO DE CAMBIO, AUT, MAN, SEMI-AUT
	- VEI_BANCOURO INT NOT NULL
	- VEI_ARCOND INT NOT NULL
	- VEI_DIRHIDRAU INT NOT NULL
	- VEI_VIDROELEC INT NOT NULL
	- VEI_SENSORRE INT NOT NULL
	- VEI_CAMERARE INT NOT NULL
	- VEI_DISPONIVEL INT NOT NULL
	- VEI_PLACA VARCHAR(6) NOT NULL UNIQUE
	- VEI_CILINDRADAS INT
	- VEI_PRECODIARIA DECIMAL(10,2) NOT NULL

TB_TIPO_VEICULO
	- TVI_ID SERIAL PK 
	- TVI_TIPO VARCHAR(20) NOT NULL         * MOTO OU SUV, CAMIONETA, SEDAN, HATCH

TB_PESSOA
	- PES_ID SERIAL PK
	- PES_NOME VARCHAR(100) NOT NULL
	- PES_NRODOC VARCHAR(14) NOT NULL UNIQUE
	- PES_NROCNH VARCHAR(10) NOT NULL UNIQUE
	- PES_DATANAS DATE
	- PES_EPEID INT FK NOT NULL * 		ENDERECO PESSOA
	- PES_TEL VARCHAR(20) 
	- PES_EMAIL VARCHAR(50)
	- PES_DATCADASTRO DATE NOT NULL

TB_ENDERECO_PESSOA
	- EPE_ID SERIAL PK
	- EPE_NOMRUA VARCHAR(100) NOT NULL
	- EDE_NRO VARCHAR(20) NOT NULL
	- EDE_COMPLEMENTO VARCHAR(50)
	- EDE_CEP VARCHAR(9) NOT NULL
	- EDE_MUNID INT FK NOT NULL *		MUNICIPIO
	
TB_MUNICIPIO
	- MUN_ID SERIAL PK
	- MUN_NOME VARCHAR(40) NOT NULL
	- MUN_EFEID INT FK NOT NULL *		ESTADO DA FEDERAÇÃO

TB_ESTADO_FEDERACAO
	- EFE_ID SERIAL PK
	- EFE_NOME VARCHAR(40) NOT NULL
	- EFE_UF CHAR(2) NOT NULL
	- EFE_PAIS VARCHAR(50) NOT NULL

TB_PRODUTOS
	- PRO_ID SERIAL PK
	- PRO_NOME VARCHAR(50) NOT NULL
	- PRO_DESC VARCHAR(255) NOT NULL
	- PRO_PRECO DECIMAL(10,2) NOT NULL
	- PRO_QTDDISP INT
	
TB_LOCACOES
	- LOC_ID SERIAL PK
	- LOC_PESID INT FK NOT NULL *       	PESSOA
	- LOC_VEIID INT FK NOT NULL * 		VEICULO
	- LOC_DATINI DATE NOT NULL
	- LOC_DATFIM DATE 
	- LOC_DIASLOCADOS INT NOT NULL
	- LOC_TOTALBRUTO DECIMAL(10,2) NOT NULL
	- LOC_TOTALLIQUIDO DECIMAL(10,2) NOT NULL

TB_PRODUTOS_LOCADOS
	- PLO_ID SERIAL PK
	- PLO_PROID INT FK NOT NULL *
	- PLO_LOCID INT FK NOT NULL *

TB_DESCONTOS_ACRESCIMOS
	- DAC_ID SERIAL PK    			DESCONTO EM R$
	- DAC_NOME VARCHAR(20) NOT NULL         ENUM DESCONTO, ACRESCIMO
	- DAC_DESCRICAO VARCHAR(100) NOT NULL
	- DAC_VALORDESACR DECIMAL(10,2) NOT NULL

TB_JUROS_MULTA					JUROS EM %
	- JMU_ID SERIAL PK
	- JMU_CODJUROS VARCHAR(6) NOT NULL
	- JMU_DESCRICAO VARCHAR(100) NOT NULL
	- JMU_PORCENTJUROS DECIMAL(1,4) NOT NULL


TB_MULTA_LOCACOES
	- MLO_ID SERIAL PK
	- MLO_LOCID INT FK NOT NULL *		ID LOCACOES
	- MLO_JMUID INT FK NOT NULL *		JUROS MULTA
						*UPDATE POR LOCID QUE É UNICO

TB_DESACR_LOCACOES
	- DLO_ID SERIAL PK
	- DLO_LOCID INT FK NOT NULL *		ID LOCACOES
	- DLO_DACID INT FK NOT NULL *		DESCONTOS ACRECIMOS
						*UPDATE POR LOCID QUE É UNICO

TB_LOCACOES_PAG
	- LPA_ID SERIAL PK
	- LPA_LOCID INT FK NOT NULL		ID LOCACOES
	- LPA_SITPAG VARCHAR(20) NOT NULL 	ENUM COM PAGA, PARCIAL, PENDENTE
	- LPA_VALPAGO DECIMAL(10,2)











